# Collector Requirements
httpx>=0.27.0,<1.0.0
pydantic>=2.6.0,<3.0.0
pytest>=8.0.0,<9.0.0

# Windows specific (required when running on Windows endpoint)
# pywin32>=306; sys_platform == 'win32'
# pyinstaller>=6.4.0
